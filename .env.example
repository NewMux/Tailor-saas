# PostgreSQL connection string. For local dev this points at the docker-compose
# postgres service; in Railway, reference the Postgres plugin's DATABASE_URL
# instead (see DEPLOYMENT.md). The password must be URL-encoded if it
# contains URL-reserved characters.
DATABASE_URL=postgres://erp:change-me@postgres:5432/tailor_erp
POSTGRES_DB=tailor_erp
POSTGRES_USER=erp
POSTGRES_PASSWORD=change-me

# Public site URL used to build password-reset and staff-invite links.
# Frontend and API are served from this same origin.
AUTH_BASE_URL=https://erp.example.com

# Optional: only needed if the frontend is ever served from a different origin
# than the API (not the case in the default single-origin deployment) - e.g.
# the split Netlify-frontend/Railway-backend setup in DEPLOYMENT.md.
# Comma-separate multiple origins if set.
ALLOWED_ORIGIN=

NODE_ENV=production
PORT=3000

# Leave unset for a single-origin deployment (frontend and API share the same
# domain, so the client defaults to same-origin requests). Set this to the
# backend's URL if the frontend is deployed separately (e.g. on Netlify) -
# see DEPLOYMENT.md.
VITE_API_URL=

# Optional: only needed if the staff-document storage proxy is used.
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# Optional: structured log verbosity (default "info" in production, "debug" otherwise).
LOG_LEVEL=info

# Optional: error monitoring. Leave unset to disable Sentry entirely.
SENTRY_DSN=
