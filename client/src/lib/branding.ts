// This repo is a per-customer template: after a sales contract is signed,
// these values (and the /brand assets) get edited for that customer's own
// deployment. The public marketing site lives in a separate repo.
export const clientBrand = {
  name: "Tafsell",
  logoSrc: "/brand/tafsell-logo-512.jpg",
  logoAlt: "Tafsell logo",
} as const;
